#include <bits/stdc+.h>
using namespace std;

#define ll long long int

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);

	int n; cin >> n;
	vector<ll> ans;
	do {
		if (n & 1) {
			n /= 2;
			ans 
		}
	} while (n != 1);

	return 0;
}